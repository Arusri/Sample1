package packone;

import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.FindBy;

public class Telugu_Page {
	
	public @FindBy(id="REGISTERED_BY") WebElement profile_for;
	public @FindBy(id="NAME") WebElement name;
}
